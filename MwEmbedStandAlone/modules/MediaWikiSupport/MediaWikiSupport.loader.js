
// Wrap in mediaWiki 
( function( mw ) {

	// Add MediaWikiSupportPlayer depenency on players with apiTitleKey 
	$( mw ).bind( 'EmbedPlayerUpdateDependencies', function( event, embedPlayer, dependencySet ){
		
		
	});

} )( window.mediaWiki );